# CT Project (BruinML)

### Get Started

1. Create a new conda env (or some other virtual env)
 - We use 3.13.7
2. Run ```bash pip install -r requirements.txt ```
3. Run ```bash python compare_methods.py ``` to compare various methods of image generation.